<h1>Eat-Da-Burger!</h1>

<img src="assets/css/high-res-combo.jpg" alt="Burger" width="30%" height="30%" class="center">

<div id="readySection" class="center">
<h2>Burgers - hot and ready to eat!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>
</div>

<div id="devouredSection" class="center">
<h2>Burgers that have been eaten!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>
</div>

<h2>Add a Burger</h2>
<div id="addSection">
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">Devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> Devoured!<br>
    <input type="radio" name="devoured" value="0"> Ready to Eat!
  </div>

  <button class="submit" type="submit">Add Burger</button>
</form>
</div>
